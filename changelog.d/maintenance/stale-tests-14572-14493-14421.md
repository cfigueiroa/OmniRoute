- **test:** realign three unit guards with intentional changes that landed without them — the Responses keepalive frame now carries `sequence_number` and `response` (#14572), the Vitest exclusion inventory is empty after #14493 restored every quarantined suite, and the Home settings read goes through `loadHomeSettings()` (#14421) while staying in the same `Promise.all` batch
